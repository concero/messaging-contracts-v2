

## Import sorting
- External libraries
- Common directory imports (e.g. `../../common/`)
- Module's libraries (e.g. `../libraries`)
- Module's components (e.g. `../Errors.sol`)
- Interfaces (e.g. `../interfaces/IERC20.sol`)
- Modules (e.g. `./Staking.sol`)
